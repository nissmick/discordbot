{
	"name": "database",
	"module": "index.ts",
	"type": "module",
	"devDependencies": {
		"@types/bun": "latest"
	},
	"peerDependencies": {
		"typescript": "^5.0.0"
	},
	"workspaces": [
		"packages/*"
	],
	"engines": {
		"bun": "*"
	},
	"scripts": {
		"preinstall": "rm bun.lockb",
		"postinstall": "chmod +x postinstall && ./postinstall",
		"start": "bun core start",
		"dev": "bun core dev",
		"db:push": "bun prisma db push",
		"db:studio": "bun prisma studio",
		"prisma": "cd packages/database && prisma",
		"core": "cd packages/core && bun",
		"sqlite": "sqlite3 packages/database/bot.db",
		"api": "cd packages/api && bun"
	}
}